# Network Scanner

      simple Network scanner using python



# Setup
    >> pip install -r ./requirements.txt
    >> (environment externally managed error ) pip install -r requirements.txt --break-system-packages   ; or create python env
    >> sudo chmod +x scannet
    >> sudo ./scannet -i [interface] OR ./scannet -r [subnet]

    
