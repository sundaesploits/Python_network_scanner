# Network Scanner

      simple Network scanner using python

# Make globally available
    >> chmod +x scannet
    >> ./scannet -g

    >> scannet [options]

# Setup

    >> chmod +x scannet
    >> ./scannet -i [interface] OR ./scannet -r [subnet]

    
